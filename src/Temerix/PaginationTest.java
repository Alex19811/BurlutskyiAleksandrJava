/*
package Temerix;

public class PaginationTest extends TestBase {

    //  1 Найти элемент, который должен исчезнуть
    //  2 Выполнить действие
    //  3 Подождать пока элемент исчезнет
    //   4 Подождать пока появится новый элемент

    @Test
    public void myFirstTest() {
        driver.navigate().to("http://paginationjs.com/");
        WebElement demo1 = driver.findElement(By.cssSelector("#demo1"));
        List<NebElement> items = demo1.findElements(By.cssSelector(".data-container ul li"));
        ListkWebElement > pagination = demo1.findElements(By.cssSelector(" paginationjs-pages ul li"));

        pagination.get(2).click);
        wait.until(ExpectedConditions.stalenessof(items.get(0)));
        items = demol.findElerenta(By.essSelector("-data-container ul 1i"));
        items = Assert.assertTrue(items.get(0).getText().equals("11"));

    }
}
*/
